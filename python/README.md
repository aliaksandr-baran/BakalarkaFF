# Python port (planned)

This folder is reserved for the Python re-implementation of the separation model,
the final step of the **Excel → MATLAB → Python** migration.

Planned scope:

- NRTL activity-coefficient model.
- LLE equilibrium solver (port of [../matlab/LLE_solver.m](../matlab/LLE_solver.m)).
- VLE calculations and process balances.

Nothing here yet — start by translating the MATLAB scripts in [../matlab](../matlab).
